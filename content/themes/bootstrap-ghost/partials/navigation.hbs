<!-- Ghost will load this instead of it's default template -->
<ul class="nav navbar-nav">
    {{#foreach navigation}}
        <li class="{{#if current}} active{{/if}}">
            <a href="{{url absolute="true"}}">{{label}}</a>
        </li>
    {{/foreach}}
</ul>